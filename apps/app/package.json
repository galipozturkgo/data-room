{
  "name": "@dataroom/app",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "tags": [
      "apps:app"
    ],
    "targets": {
      "deploy": {
        "dependsOn": [
          "build"
        ],
        "executor": "@dataroom/dev-executors:deploy",
        "options": {
          "dockerfile": "apps/app/Dockerfile",
          "name": "dataroom-app",
          "tag": "latest"
        }
      }
    }
  }
}
