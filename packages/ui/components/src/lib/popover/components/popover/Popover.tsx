'use client';

import { BasePopover } from '../popover-base/PopoverBase';

export const Popover = Object.assign(BasePopover, {
  Title: BasePopover.Title,
  Button: BasePopover.Button,
  Panel: BasePopover.Panel,
  Content: BasePopover.Content,
  Group: BasePopover.Group,
  Backdrop: BasePopover.Backdrop,
});
